- Install RtMidi library for MIDI I/O


- Phase modulation only works for Sine Oscillators (so that it behaves like FM), not for any other

- Seg fault when destructor of Feedback is called

- Redesign: Remove block header completely. Since only operators are used in the structures anyway Block.h is useless.
- Do something about the horrible use of pointers